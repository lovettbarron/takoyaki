---
phase: 02-read-only-browser
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - src/components/project-detail/ProjectDetailView.tsx
  - src/components/project-detail/MetadataHeader.tsx
  - src/components/project-detail/BanksTab.tsx
  - src/components/project-detail/BankGridCell.tsx
  - src/components/project-detail/SamplesTab.tsx
  - src/components/project-detail/SlotRow.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - BROW-03
  - BROW-04
  - BROW-05

must_haves:
  truths:
    - "User can open a project and see a breadcrumb trail (Projects > PROJECT_NAME)"
    - "User sees a metadata header with project name, tempo (120.0 BPM format), bank count, and last modified date"
    - "User can switch between Banks, Samples, and Health tabs"
    - "Banks tab shows a 4x4 grid with filled dots for populated banks and empty dots for unused banks"
    - "User can click a populated bank to drill into 4 parts and 8 tracks per part with machine type and sample filename"
    - "Samples tab shows Flex and Static sections with slot number, filename, sample rate, and status icon per row"
    - "Empty sample slots are hidden by default with a Show all slots toggle to reveal them"
    - "User can expand a sample slot row to see which banks/parts/tracks reference that sample"
  artifacts:
    - path: "src/components/project-detail/ProjectDetailView.tsx"
      provides: "Project detail shell with breadcrumb, metadata header, and tab container"
      contains: "useQuery"
    - path: "src/components/project-detail/MetadataHeader.tsx"
      provides: "Always-visible project metadata strip"
      contains: "BPM"
    - path: "src/components/project-detail/BanksTab.tsx"
      provides: "4x4 bank grid with drill-down panel"
      contains: "grid-cols-4"
    - path: "src/components/project-detail/BankGridCell.tsx"
      provides: "Individual bank grid cell with populated/empty state"
      contains: "rounded-full"
    - path: "src/components/project-detail/SamplesTab.tsx"
      provides: "Flex and Static sample slot tables with show/hide toggle"
      contains: "FLEX SAMPLES"
    - path: "src/components/project-detail/SlotRow.tsx"
      provides: "Sample slot row with collapsible cross-reference detail"
      contains: "Collapsible"
  key_links:
    - from: "src/components/project-detail/ProjectDetailView.tsx"
      to: "src/lib/tauri.ts"
      via: "useQuery calls getProjectDetail(selectedProjectId)"
      pattern: "getProjectDetail"
    - from: "src/components/project-detail/BanksTab.tsx"
      to: "src/lib/stores/navigation.ts"
      via: "selectBank(bankIndex) updates selected bank"
      pattern: "selectBank"
    - from: "src/components/project-detail/SamplesTab.tsx"
      to: "src/lib/tauri.ts"
      via: "useQuery calls getProjectSamples(selectedProjectId)"
      pattern: "getProjectSamples"
    - from: "src/app/page.tsx"
      to: "src/components/project-detail/ProjectDetailView.tsx"
      via: "view === 'project-detail' renders ProjectDetailView"
      pattern: "ProjectDetailView"
---

<objective>
Build the project detail view with breadcrumb navigation, metadata header, tabbed layout (Banks/Samples/Health), the 4x4 bank grid with drill-down, and the sample slot tables with cross-reference expansion. This is the core browsing experience for exploring an individual OT project.

Purpose: Per D-03 through D-10, users need full visibility into their OT project structure: which banks are populated, what machine types are assigned to each track, and which sample files are in each slot. The bank grid mirrors the OT's own 4x4 layout for intuitive recognition. The sample slot tables provide at-a-glance status with expandable cross-reference detail.
Output: A working project detail view with three tabs (Banks, Samples, Health placeholder) and complete drill-down functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-only-browser/02-RESEARCH.md
@.planning/phases/02-read-only-browser/02-PATTERNS.md
@.planning/phases/02-read-only-browser/02-UI-SPEC.md
@.planning/phases/01-foundation/01-CONTEXT.md

@.planning/phases/02-read-only-browser/02-03-SUMMARY.md

<interfaces>
<!-- Types and stores from Plan 03 -->

From src/lib/types.ts:
```typescript
export interface ProjectDetail {
  project_name: string;
  tempo_bpm: number | null;
  bank_count: number | null;
  last_modified: string | null;
  banks: BankDetail[];
}

export interface BankDetail {
  bank_index: number;
  populated: boolean;
  bank_name: string | null;
  parts: PartDetail[];
}

export interface PartDetail {
  part_index: number;
  part_name: string | null;
  tracks: TrackDetail[];
}

export interface TrackDetail {
  track_index: number;
  machine_type: string;
  sample_slot_index: number | null;
  sample_filename: string | null;
}

export interface SampleSlotResponse {
  flex: SampleSlot[];
  static_slots: SampleSlot[];
}

export interface SampleSlot {
  slot_index: number;
  occupied: boolean;
  filename: string | null;
  full_path: string | null;
  sample_rate: number | null;
  status: string;
}
```

From src/lib/tauri.ts:
```typescript
export async function getProjectDetail(projectId: string): Promise<ProjectDetail>;
export async function getProjectSamples(projectId: string): Promise<SampleSlotResponse>;
export async function runHealthCheck(projectId: string): Promise<void>;
```

From src/lib/stores/navigation.ts:
```typescript
export const useNavigationStore: {
  view: View;
  selectedProjectId: string | null;
  selectedBankIndex: number | null;
  activeTab: "banks" | "samples" | "health";
  navigateToProject: (projectId: string) => void;
  navigateToList: () => void;
  selectBank: (bankIndex: number | null) => void;
  setActiveTab: (tab: "banks" | "samples" | "health") => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProjectDetailView shell with breadcrumb, metadata header, and tabs</name>
  <files>src/components/project-detail/ProjectDetailView.tsx, src/components/project-detail/MetadataHeader.tsx, src/app/page.tsx</files>
  <read_first>
    - /Users/albair/src/takoyaki/src/app/page.tsx
    - /Users/albair/src/takoyaki/src/lib/stores/navigation.ts
    - /Users/albair/src/takoyaki/src/lib/tauri.ts
    - /Users/albair/src/takoyaki/src/lib/types.ts
    - /Users/albair/src/takoyaki/.planning/phases/02-read-only-browser/02-UI-SPEC.md (Project Detail View layout, Breadcrumb section, Metadata Header section, Tabs section, Interaction Contract, Copywriting Contract - Project Detail)
    - /Users/albair/src/takoyaki/.planning/phases/02-read-only-browser/02-PATTERNS.md (ProjectDetailView, MetadataHeader pattern assignments)
  </read_first>
  <action>
Install shadcn components needed for the detail view:
```bash
npx shadcn@latest add tabs breadcrumb tooltip
```

**Create `src/components/project-detail/MetadataHeader.tsx`:**

"use client" component. Per D-07, a compact always-visible strip below the breadcrumb. Props: `project: ProjectDetail`. Fixed height h-12 (48px), `px-4 py-2`, `border-b border-[hsl(30,8%,26%)]`. Contains:
- Left: project name in `font-mono text-2xl font-semibold text-foreground` (Display size from UI-SPEC)
- Right: metadata items separated by ` . ` (middle dot with spaces):
  - Tempo: `{project.tempo_bpm !== null ? project.tempo_bpm.toFixed(1) : "--"} BPM` in `font-mono text-sm text-muted-foreground`
  - Bank count: `{project.bank_count ?? 0} banks` in `text-sm text-muted-foreground`
  - Modified: `Modified {dateString}` in `text-sm text-muted-foreground`

**Create `src/components/project-detail/ProjectDetailView.tsx`:**

"use client" component. Per D-03 and D-04, the main detail shell. Uses `useNavigationStore()` to get `selectedProjectId`, `activeTab`, `setActiveTab`, `navigateToList`.

Data fetching:
```typescript
const { data: project, isPending } = useQuery({
  queryKey: ["project", selectedProjectId],
  queryFn: () => getProjectDetail(selectedProjectId!),
  enabled: selectedProjectId !== null,
});
```

Also trigger health check on project open (D-11):
```typescript
useEffect(() => {
  if (selectedProjectId) {
    runHealthCheck(selectedProjectId).catch(() => {});
  }
}, [selectedProjectId]);
```

Layout structure:
1. **Breadcrumb** -- shadcn `Breadcrumb` with items: "Projects" (clickable, calls `navigateToList()`, accent color `text-[hsl(38,85%,55%)]`) > project_name (current, not clickable, `text-muted-foreground`). If `selectedBankIndex !== null`, add third item: "Bank {N+1}". Padding: `px-4 py-2`.
2. **MetadataHeader** -- `<MetadataHeader project={project} />`
3. **Tabs** -- shadcn `Tabs` with `value={activeTab}` and `onValueChange={setActiveTab}`:
   - Tab "banks" label: "Banks"
   - Tab "samples" label: "Samples"
   - Tab "health" label: "Health" + Badge showing issue count (from react-query cache `["health", selectedProjectId]`)
   - Active tab has accent bottom border: use `data-[state=active]:border-b-2 data-[state=active]:border-[hsl(38,85%,55%)]`
4. **Tab content**:
   - `activeTab === "banks"` -> `<BanksTab project={project} />` (created in Task 2)
   - `activeTab === "samples"` -> `<SamplesTab projectId={selectedProjectId} />` (created in Task 2)
   - `activeTab === "health"` -> placeholder `<div>Health tab (Plan 05)</div>` (created in Plan 05)

Loading state: Skeleton fills metadata header area and tab content area when `isPending` is true.

**Update `src/app/page.tsx`:**

Replace the placeholder `<div>Project Detail (Plan 04)</div>` with `<ProjectDetailView />` import and render when `view === "project-detail"`.
  </action>
  <verify>
    <automated>cd /Users/albair/src/takoyaki && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/project-detail/ProjectDetailView.tsx contains `useQuery`
    - src/components/project-detail/ProjectDetailView.tsx contains `getProjectDetail`
    - src/components/project-detail/ProjectDetailView.tsx contains `runHealthCheck`
    - src/components/project-detail/ProjectDetailView.tsx contains `Breadcrumb`
    - src/components/project-detail/ProjectDetailView.tsx contains `Tabs`
    - src/components/project-detail/ProjectDetailView.tsx contains `activeTab`
    - src/components/project-detail/ProjectDetailView.tsx contains `navigateToList`
    - src/components/project-detail/MetadataHeader.tsx contains `toFixed(1)` (tempo one decimal)
    - src/components/project-detail/MetadataHeader.tsx contains `BPM`
    - src/components/project-detail/MetadataHeader.tsx contains `Modified`
    - src/app/page.tsx contains `ProjectDetailView`
    - TypeScript compilation has no errors related to these files
  </acceptance_criteria>
  <done>ProjectDetailView renders with breadcrumb navigation, metadata header strip (name, tempo, banks, modified), and three-tab layout. Health check triggers automatically on project open. Breadcrumb "Projects" link returns to list. Metadata header shows tempo with one decimal place.</done>
</task>

<task type="auto">
  <name>Task 2: Build Banks tab (4x4 grid + drill-down) and Samples tab (slot tables + cross-reference)</name>
  <files>src/components/project-detail/BanksTab.tsx, src/components/project-detail/BankGridCell.tsx, src/components/project-detail/SamplesTab.tsx, src/components/project-detail/SlotRow.tsx</files>
  <read_first>
    - /Users/albair/src/takoyaki/src/components/project-detail/ProjectDetailView.tsx (just created in Task 1)
    - /Users/albair/src/takoyaki/src/lib/stores/navigation.ts
    - /Users/albair/src/takoyaki/src/lib/tauri.ts
    - /Users/albair/src/takoyaki/src/lib/types.ts
    - /Users/albair/src/takoyaki/.planning/phases/02-read-only-browser/02-UI-SPEC.md (Banks Tab layout, Samples Tab layout, Bank drill-down panel, Interaction Contract, Copywriting Contract - Banks/Samples sections, State Definitions)
    - /Users/albair/src/takoyaki/.planning/phases/02-read-only-browser/02-PATTERNS.md (BanksTab, BankGridCell, SamplesTab, SlotRow pattern assignments)
  </read_first>
  <action>
Install shadcn components needed:
```bash
npx shadcn@latest add collapsible toggle
```

**Create `src/components/project-detail/BankGridCell.tsx`:**

"use client" component. Per D-06, a 48x48px cell in the 4x4 bank grid. Props: `bankIndex: number, populated: boolean, selected: boolean, onClick?: () => void`. Renders:
- `<button>` element, `type="button"`, `w-12 h-12` (48px), `flex flex-col items-center justify-center rounded border font-mono text-xs`
- `disabled={!populated}` -- empty cells get no click handler
- Populated state: `cursor-pointer hover:bg-[hsl(30,8%,20%)]` (Elevated surface), filled dot `h-2 w-2 rounded-full bg-foreground`
- Empty state: `cursor-default opacity-40`, empty dot `h-2 w-2 rounded-full border border-muted-foreground`
- Selected state: `border-[hsl(38,85%,55%)] bg-[hsl(30,8%,20%)]/30` (accent border)
- Bank number below dot: `mt-1 tabular-nums` showing `String(bankIndex + 1).padStart(2, "0")`
- `aria-label`: `Bank ${bankIndex + 1}${populated ? "" : " (empty)"}`
- `aria-pressed={selected}`

**Create `src/components/project-detail/BanksTab.tsx`:**

"use client" component. Per D-05, D-06. Props: `project: ProjectDetail`. Uses `useNavigationStore()` for `selectedBankIndex` and `selectBank()`.

Layout:
1. 4x4 CSS grid: `grid grid-cols-4 gap-2` containing 16 `BankGridCell` components
2. Below grid: `text-xs text-muted-foreground` showing "{N} of 16 banks used"
3. When no bank selected: text hint "Select a bank to see its parts and tracks." in `text-sm text-muted-foreground`
4. When bank selected (selectedBankIndex !== null):
   - Bank heading: "Bank {N+1}" + bank_name if set in `text-muted-foreground`: `Bank 01 -- BEATS_A` or just `Bank 01` if unnamed
   - Parts displayed as 4-column layout (or stacked on narrow widths): "Part 1", "Part 2", "Part 3", "Part 4"
   - Under each part, 8 tracks listed: track index (Label, muted, tabular-nums) + machine_type string (Label, muted, font-mono) + sample_filename or "--" (Body, truncated)
5. Clicking same bank again deselects: `onClick={selectedBankIndex === bankIndex ? () => selectBank(null) : () => selectBank(bankIndex)}`
6. Clicking empty cell: no action (disabled button, no onClick)

**Create `src/components/project-detail/SlotRow.tsx`:**

"use client" component. Per D-09, D-10. Props: `slot: SampleSlot, slotType: "flex" | "static", healthData?: HealthCheckComplete`. Uses shadcn `Collapsible` for expandable cross-reference detail.

Layout:
- `TableRow` with `cursor-pointer h-9`
- Cells:
  - `#`: `w-12 font-mono text-xs tabular-nums text-muted-foreground` -- `String(slot.slot_index + 1).padStart(3, "0")` (3-digit zero-padded #001-#128)
  - FILENAME: `flex-1 font-mono text-sm truncate` -- `slot.filename ?? "--"`. On hover, shadcn `Tooltip` shows `slot.full_path` (full untruncated path)
  - RATE: `w-[72px] font-mono text-sm` -- display `slot.sample_rate === 44100 ? "44.1k" : slot.sample_rate === 48000 ? "48k" : slot.sample_rate ? `${slot.sample_rate}` : "--"`
  - STATUS: `w-12 text-center` -- Lucide icons:
    - `CircleCheck` in `text-[hsl(140,60%,42%)]` (Health: OK green) for status "ok"
    - `CircleX` in `text-[hsl(0,68%,48%)]` (Health: Error red) for status "missing" or "error"
    - `CircleAlert` in `text-[hsl(38,85%,55%)]` (Health: Warning amber) for status "warning"
    - No icon for unscanned slots
    - Each icon wrapped in `Tooltip` with description text per UI-SPEC Copywriting Contract

- Collapsible content: when row is clicked/expanded, show `CollapsibleContent` with `bg-[hsl(30,8%,16%)]` (Secondary), `px-4 py-2`. If the slot has cross-reference data (from ProjectDetail banks), display "Referenced by: Bank 01 Part 1 Track 2, Bank 02 Part 1 Track 2". If no references: "Not referenced by any bank, part, or track."

**Create `src/components/project-detail/SamplesTab.tsx`:**

"use client" component. Per D-08, D-09. Props: `projectId: string`.

Data fetching:
```typescript
const { data: samples, isPending } = useQuery({
  queryKey: ["samples", projectId],
  queryFn: () => getProjectSamples(projectId),
  enabled: !!projectId,
});
```

Layout:
- Top-right: shadcn `Toggle` with text "Show all slots" (when off) / "Hide empty slots" (when on). `const [showEmpty, setShowEmpty] = useState(false);`
- "FLEX SAMPLES" section heading: `font-mono text-lg font-semibold` with `mb-2 pt-0`
- Table with columns: # (48px), FILENAME (flex-1), RATE (72px), STATUS (48px). Column headers in `font-mono text-xs font-semibold uppercase text-muted-foreground`.
- Map over `samples.flex.filter(s => showEmpty || s.occupied)` rendering `SlotRow` for each
- Separator: `pt-6` margin before Static section
- "STATIC SAMPLES" section heading: same styling
- Map over `samples.static_slots.filter(s => showEmpty || s.occupied)` rendering `SlotRow` for each
- Empty slots (when shown): slot number + "--" filename + "--" rate + no status icon, all cells in `text-muted-foreground`
- If no populated slots at all: "No samples assigned to any slot in this project." centered message
  </action>
  <verify>
    <automated>cd /Users/albair/src/takoyaki && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/project-detail/BankGridCell.tsx contains `w-12 h-12`
    - src/components/project-detail/BankGridCell.tsx contains `rounded-full`
    - src/components/project-detail/BankGridCell.tsx contains `aria-label`
    - src/components/project-detail/BankGridCell.tsx contains `disabled={!populated}`
    - src/components/project-detail/BanksTab.tsx contains `grid grid-cols-4`
    - src/components/project-detail/BanksTab.tsx contains `selectBank`
    - src/components/project-detail/BanksTab.tsx contains `Select a bank to see its parts and tracks`
    - src/components/project-detail/BanksTab.tsx contains `of 16 banks used`
    - src/components/project-detail/SamplesTab.tsx contains `FLEX SAMPLES`
    - src/components/project-detail/SamplesTab.tsx contains `STATIC SAMPLES`
    - src/components/project-detail/SamplesTab.tsx contains `Show all slots`
    - src/components/project-detail/SamplesTab.tsx contains `getProjectSamples`
    - src/components/project-detail/SlotRow.tsx contains `Collapsible`
    - src/components/project-detail/SlotRow.tsx contains `padStart(3`
    - src/components/project-detail/SlotRow.tsx contains `CircleCheck` or `CircleX` or `CircleAlert`
    - TypeScript compilation has no errors related to these files
  </acceptance_criteria>
  <done>Banks tab shows 4x4 grid with populated/empty dots. Clicking a populated bank shows 4 parts with 8 tracks each. Samples tab shows Flex and Static sections with slot number, filename, rate, and status icon. Empty slots hidden by default with toggle. Slot rows expand to show cross-reference detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust IPC -> React components | Project detail and sample data from Rust backend rendered in UI |
| User interaction -> Navigation state | Click events update zustand store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information Disclosure | SlotRow.tsx full_path tooltip | accept | Sample file paths shown in tooltip are local filesystem paths on user's own machine. Desktop app, no network exposure. Low risk. |
| T-02-11 | Spoofing | ProjectDetailView.tsx | accept | Project data comes from Rust backend (trusted). No user-editable fields in Phase 2 (read-only). |
</threat_model>

<verification>
1. `npx tsc --noEmit` compiles without errors
2. Bank grid renders 4x4 with correct populated/empty states
3. Clicking populated bank shows drill-down with parts and tracks
4. Samples tab shows Flex and Static sections
5. Toggle switches between hiding and showing empty slots
6. Slot rows expand to show cross-reference data
7. Breadcrumb "Projects" navigates back to list
</verification>

<success_criteria>
Users can view full project structure: 4x4 bank grid matching OT layout, drill-down into parts/tracks with machine types, and sample slot tables with expandable cross-reference. Tempo displays with one decimal. Empty slots hidden by default. All interactions match the UI-SPEC interaction contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-read-only-browser/02-04-SUMMARY.md`
</output>

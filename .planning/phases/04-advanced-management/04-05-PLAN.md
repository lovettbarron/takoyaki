---
phase: 04-advanced-management
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified: []
autonomous: false
requirements: [MGMT-01, MGMT-02, MGMT-03, SMPL-02]
must_haves:
  truths:
    - "User can rename a project and see the dry-run preview before it applies"
    - "User can duplicate a project with default _copy suffix"
    - "User can export a project to ~/takoyaki/exports/ as a playable zip"
    - "User can right-click a bank and copy it to another project"
    - "All operations create a pre-operation snapshot"
    - "Success banners appear after each completed operation"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint: human confirms all four management operations work correctly in the running application.

Purpose: Verify the full vertical slice -- from UI trigger through IPC to Rust backend and back. Catches integration issues that unit tests miss (wiring, event streaming, visual rendering).

Output: Verified working Phase 4 management operations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-management/04-03-SUMMARY.md
@.planning/phases/04-advanced-management/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile and run full test suite</name>
  <files></files>
  <read_first>
    crates/takoyaki-app/src/lib.rs
    src/app/page.tsx
  </read_first>
  <action>
1. Run `cargo check -p takoyaki-app` to verify Rust compilation
2. Run `npx tsc --noEmit` to verify TypeScript compilation
3. Run `cargo test -p takoyaki-app management` to verify all management tests pass
4. Run `cargo test -p takoyaki-app` to verify no regressions in existing tests
5. Fix any compilation errors or test failures discovered during integration
  </action>
  <verify>
    <automated>cd /Users/albair/src/takoyaki && cargo check -p takoyaki-app 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5 && cargo test -p takoyaki-app 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo check -p takoyaki-app exits 0
    - npx tsc --noEmit exits 0
    - cargo test -p takoyaki-app exits 0 with no failures
  </acceptance_criteria>
  <done>Application compiles, type-checks, and all tests pass (both new management tests and existing Phase 1-3 tests).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all management operations</name>
  <files></files>
  <action>
Launch the Tauri application and verify all four management operations end-to-end. The executor should start the app with `cargo tauri dev` and walk through each operation in the running UI. All operations must go through the dry-run preview modal before executing, show progress during execution, and display a success banner on completion.

Specific verification steps:

**Rename (MGMT-02):**
1. Open a project detail view
2. Click "Rename" toolbar button -- project name becomes editable inline input with amber underline
3. Type invalid characters (spaces, special chars) -- they are blocked by the OT character filter
4. Type a valid new name, press Enter -- dry-run preview modal appears showing "Rename OLD_NAME -> NEW_NAME"

**Duplicate (MGMT-01):**
1. Click "Duplicate" toolbar button -- dry-run preview appears showing files to be created
2. If default name exceeds 16 chars, user is prompted for a shorter name

**Export (MGMT-03):**
1. Click "Export" toolbar button -- dry-run preview appears listing project + audio files
2. Confirm -- progress indicator appears during zip creation
3. After completion: success banner shows with ~/takoyaki/exports/ path
4. Verify zip file exists at the path

**Bank Copy (SMPL-02):**
1. Right-click a populated bank cell -- context menu appears with "Copy to project..."
2. Click it -- two-step picker dialog opens (Step 1: project list, Step 2: 4x4 bank grid)
3. If target slot is populated, warning text appears
4. Click "Copy Bank" -- dry-run preview appears

**General:**
- Success banners auto-dismiss after ~4 seconds
- All buttons have correct icons (Pencil, Copy, PackageOpen, ArrowRightFromLine)
- Right-clicking empty bank cells does NOT show context menu
  </action>
  <verify>
    <automated>cd /Users/albair/src/takoyaki && cargo check -p takoyaki-app 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All four operations trigger dry-run preview before execution
    - Rename inline edit validates OT characters in real-time
    - Bank copy context menu only appears on populated cells
    - Success banners appear and auto-dismiss
    - No console errors during operation execution
  </acceptance_criteria>
  <done>All four Phase 4 management operations verified working end-to-end in the running application. Dry-run preview, snapshots, progress, and success banners all functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan, no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new attack surface -- verification only |
</threat_model>

<verification>
- All Rust and TypeScript code compiles without errors
- All tests pass (management + existing)
- Human confirms all four operations work end-to-end
</verification>

<success_criteria>
- Rename, Duplicate, Export, and Bank Copy all function correctly from UI through to backend
- Dry-run preview shows before every operation
- Pre-operation snapshots are created (visible in snapshot history)
- Success banners appear and auto-dismiss
- No regressions in existing Phase 1-3 functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-management/04-05-SUMMARY.md`
</output>

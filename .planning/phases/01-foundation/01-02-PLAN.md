---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.mjs
  - tsconfig.json
  - components.json
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/providers.tsx
  - src/lib/utils.ts
autonomous: true
requirements:
  - FNDN-06

must_haves:
  truths:
    - "npm run build produces a static export in the out/ directory"
    - "The app uses Iosevka as primary monospace font"
    - "The warm dark palette CSS variables are defined in globals.css"
    - "shadcn components are available (button, dialog, sidebar, sonner, separator, skeleton, badge)"
  artifacts:
    - path: "package.json"
      provides: "Frontend dependencies"
      contains: "@tanstack/react-query"
    - path: "next.config.mjs"
      provides: "Static export configuration"
      contains: "output: 'export'"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 theme with warm dark palette"
      contains: "--background: hsl(30 8% 10%)"
    - path: "src/app/layout.tsx"
      provides: "Root layout with providers and Iosevka font"
      contains: "iosevka"
    - path: "src/components/providers.tsx"
      provides: "React Query provider"
      contains: "QueryClientProvider"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers.tsx"
      via: "Providers component wrapping children"
      pattern: "<Providers>"
    - from: "src/app/globals.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: '@import "tailwindcss"'
---

<objective>
Set up the Next.js frontend with Tailwind v4, shadcn component library, Iosevka font, and the warm dark color palette that defines Takoyaki's visual identity.

Purpose: Establishes the frontend build pipeline and design system tokens. Per D-05/D-06/D-08, the app uses a monome-inspired warm dark palette with monospace-forward typography. All future UI work builds on these tokens and components.
Output: A building Next.js static export with shadcn initialized and warm dark theme applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- This plan creates frontend infrastructure. No prior interfaces needed. -->

Wallflower next.config.mjs (exact copy):
```js
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
};
export default nextConfig;
```

Wallflower providers.tsx pattern (exact copy):
```tsx
"use client";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useState, type ReactNode } from "react";

export function Providers({ children }: { children: ReactNode }) {
  const [queryClient] = useState(() => new QueryClient({
    defaultOptions: { queries: { retry: 1, refetchOnWindowFocus: false } },
  }));
  return <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>;
}
```

CSS variable tokens from UI-SPEC.md:
```
--background: hsl(30 8% 10%)     /* #1c1a18 */
--foreground: hsl(30 10% 88%)    /* #e3ddd6 */
--card: hsl(30 8% 16%)           /* #2a2723 */
--accent: hsl(38 85% 55%)        /* #f0a832 */
--muted: hsl(30 8% 18%)
--muted-foreground: hsl(30 8% 50%)
--border: hsl(30 8% 26%)
--destructive: hsl(0 68% 48%)
--radius: 0.375rem
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with dependencies and configuration</name>
  <files>package.json, next.config.mjs, tsconfig.json, postcss.config.mjs, src/lib/utils.ts, src/components/providers.tsx</files>
  <read_first>
    - /Users/albair/src/takoyaki/CLAUDE.md
    - /Users/albair/src/takoyaki/.planning/phases/01-foundation/01-PATTERNS.md (package.json pattern, next.config.mjs pattern, tsconfig.json pattern, providers.tsx pattern, utils.ts pattern)
    - /Users/albair/src/takoyaki/.planning/phases/01-foundation/01-RESEARCH.md (Standard Stack — npm versions, Installation section)
  </read_first>
  <action>
Create `package.json`:
```json
{
  "name": "takoyaki",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@fontsource/iosevka": "latest",
    "@tanstack/react-query": "^5.100.6",
    "@tauri-apps/api": "^2.10.1",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.8.0",
    "next": "^16.2.4",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "shadcn": "^4.3.0",
    "sonner": "^2.0.7",
    "tailwind-merge": "^3.5.0",
    "tw-animate-css": "^1.4.0",
    "zustand": "^5.0.12"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.2",
    "@tauri-apps/cli": "^2.10.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@types/node": "latest",
    "postcss": "^8.5.10",
    "tailwindcss": "^4.2.2",
    "typescript": "^6.0.3"
  }
}
```

Run `npm install` to generate `node_modules` and `package-lock.json`.

Create `next.config.mjs`:
```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
};

export default nextConfig;
```

Create `tsconfig.json` matching Wallflower's pattern — critical paths:
- `"compilerOptions.paths": { "@/*": ["./src/*"] }` for the `@/` alias convention
- `"jsx": "preserve"`, `"module": "esnext"`, `"moduleResolution": "bundler"`
- `"strict": true`

Create `postcss.config.mjs`:
```js
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
export default config;
```

Create `src/lib/utils.ts`:
```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

Create `src/components/providers.tsx`:
```tsx
"use client";

import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useState, type ReactNode } from "react";

export function Providers({ children }: { children: ReactNode }) {
  const [queryClient] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: {
            retry: 1,
            refetchOnWindowFocus: false,
          },
        },
      }),
  );

  return (
    <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
  );
}
```

Create `components.json` for shadcn:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "base-nova",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "iconLibrary": "lucide",
  "rtl": false,
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  }
}
```

Then run `npx shadcn@latest add button dialog sidebar sonner separator skeleton badge` to install shadcn components. If shadcn prompts interactively, run with `--yes` or `--overwrite` flags as needed.
  </action>
  <verify>
    <automated>cd /Users/albair/src/takoyaki && test -f node_modules/.package-lock.json && test -f components.json && test -d src/components/ui && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@fontsource/iosevka"` and `"zustand"` and `"@tanstack/react-query"`
    - next.config.mjs contains `output: 'export'`
    - tsconfig.json contains `"@/*"` path alias
    - src/lib/utils.ts contains `export function cn(`
    - src/components/providers.tsx contains `QueryClientProvider`
    - components.json contains `"style": "base-nova"`
    - src/components/ui/ directory exists with button.tsx, dialog.tsx, sidebar.tsx
    - node_modules directory exists
  </acceptance_criteria>
  <done>Next.js project initialized with all dependencies, shadcn components installed, providers and utils in place.</done>
</task>

<task type="auto">
  <name>Task 2: Create globals.css with warm dark palette and root layout with Iosevka</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    - /Users/albair/src/takoyaki/.planning/phases/01-foundation/01-UI-SPEC.md (Color section, Typography section, Layout Contract, Spacing Scale)
    - /Users/albair/src/takoyaki/.planning/phases/01-foundation/01-PATTERNS.md (globals.css pattern, layout.tsx pattern, page.tsx pattern)
    - /Users/albair/src/takoyaki/src/components/ui/sidebar.tsx (shadcn sidebar component — check what CSS variables it expects)
    - /Users/albair/src/takoyaki/src/app/globals.css (if shadcn created one during init — read before overwriting)
  </read_first>
  <action>
Create or overwrite `src/app/globals.css` with the full Tailwind v4 warm dark theme. The file must contain:

1. Tailwind imports at top:
```css
@import "tailwindcss";
@import "tw-animate-css";
```
If shadcn installed a `shadcn/tailwind.css` import during init, preserve it.

2. `@theme inline` block mapping shadcn CSS variables to Tailwind utilities:
```css
@theme inline {
    --font-mono: 'Iosevka', 'JetBrains Mono', monospace;
    --font-sans: 'Inter', system-ui, sans-serif;
    --color-background: var(--background);
    --color-foreground: var(--foreground);
    --color-card: var(--card);
    --color-card-foreground: var(--card-foreground);
    --color-popover: var(--popover);
    --color-popover-foreground: var(--popover-foreground);
    --color-primary: var(--primary);
    --color-primary-foreground: var(--primary-foreground);
    --color-secondary: var(--secondary);
    --color-secondary-foreground: var(--secondary-foreground);
    --color-muted: var(--muted);
    --color-muted-foreground: var(--muted-foreground);
    --color-accent: var(--accent);
    --color-accent-foreground: var(--accent-foreground);
    --color-destructive: var(--destructive);
    --color-destructive-foreground: var(--destructive-foreground);
    --color-border: var(--border);
    --color-input: var(--input);
    --color-ring: var(--ring);
    --color-sidebar: var(--sidebar);
    --color-sidebar-foreground: var(--sidebar-foreground);
    --color-sidebar-primary: var(--sidebar-primary);
    --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
    --color-sidebar-accent: var(--sidebar-accent);
    --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
    --color-sidebar-border: var(--sidebar-border);
    --color-sidebar-ring: var(--sidebar-ring);
    --radius-sm: calc(var(--radius) - 2px);
    --radius-md: var(--radius);
    --radius-lg: calc(var(--radius) + 2px);
    --radius-xl: calc(var(--radius) + 4px);
}
```

3. `:root` block with ALL warm dark palette values per UI-SPEC.md:
```css
:root {
    --background: hsl(30 8% 10%);
    --foreground: hsl(30 10% 88%);
    --card: hsl(30 8% 16%);
    --card-foreground: hsl(30 10% 88%);
    --popover: hsl(30 8% 16%);
    --popover-foreground: hsl(30 10% 88%);
    --primary: hsl(38 85% 55%);
    --primary-foreground: hsl(0 0% 100%);
    --secondary: hsl(30 8% 16%);
    --secondary-foreground: hsl(30 10% 88%);
    --muted: hsl(30 8% 18%);
    --muted-foreground: hsl(30 8% 50%);
    --accent: hsl(38 85% 55%);
    --accent-foreground: hsl(0 0% 100%);
    --destructive: hsl(0 68% 48%);
    --destructive-foreground: hsl(0 0% 100%);
    --border: hsl(30 8% 26%);
    --input: hsl(30 8% 26%);
    --ring: hsl(38 85% 55%);
    --radius: 0.375rem;
    --sidebar: hsl(30 8% 14%);
    --sidebar-foreground: hsl(30 10% 88%);
    --sidebar-primary: hsl(38 85% 55%);
    --sidebar-primary-foreground: hsl(0 0% 100%);
    --sidebar-accent: hsl(30 8% 20%);
    --sidebar-accent-foreground: hsl(30 10% 88%);
    --sidebar-border: hsl(30 8% 22%);
    --sidebar-ring: hsl(38 85% 55%);
}
```

4. `@layer base` block with Takoyaki-specific overrides:
```css
@layer base {
    * {
        @apply border-border outline-ring/50;
    }
    body {
        @apply bg-background text-foreground;
        -webkit-user-select: none;
        user-select: none;
        -webkit-tap-highlight-color: transparent;
        overflow: hidden;
        height: 100vh;
        font-family: var(--font-mono), monospace;
    }
    html {
        overflow: hidden;
        overscroll-behavior: none;
        height: 100vh;
    }
    input, textarea, [contenteditable="true"] {
        -webkit-user-select: text;
        user-select: text;
    }
}
```
NOTE: `font-family: var(--font-mono)` on body implements D-08 (monospace-forward). Wallflower uses `--font-sans` here instead.

Create `src/app/layout.tsx`:
```tsx
import type { Metadata } from "next";
import "./globals.css";
import "@fontsource/iosevka/400.css";
import "@fontsource/iosevka/500.css";
import "@fontsource/iosevka/600.css";
import { Providers } from "@/components/providers";
import { Toaster } from "@/components/ui/sonner";

export const metadata: Metadata = {
  title: "Takoyaki",
  description: "Octatrack backup and project manager",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <head>
        <meta name="color-scheme" content="dark" />
      </head>
      <body className="min-h-screen">
        <Providers>
          <div id="main-content" className="h-screen overflow-hidden">
            {children}
          </div>
          <Toaster position="bottom-right" />
        </Providers>
      </body>
    </html>
  );
}
```
NOTE: Import Iosevka weights 400 (regular) and 600 (semibold) per UI-SPEC.md Typography section. Also 500 for intermediate use. No TauriEventListener yet — added in Plan 06.

Create `src/app/page.tsx` as a minimal placeholder:
```tsx
export default function Home() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-muted-foreground text-sm font-mono">Takoyaki</p>
    </div>
  );
}
```
This placeholder will be replaced by Plan 06 with the full sidebar + disconnected state layout.
  </action>
  <verify>
    <automated>cd /Users/albair/src/takoyaki && npm run build 2>&1 | tail -5 && test -d out && echo "BUILD PASS" || echo "BUILD FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains `--background: hsl(30 8% 10%)`
    - src/app/globals.css contains `--accent: hsl(38 85% 55%)`
    - src/app/globals.css contains `font-family: var(--font-mono)`
    - src/app/globals.css contains `@import "tailwindcss"`
    - src/app/layout.tsx contains `@fontsource/iosevka`
    - src/app/layout.tsx contains `<Providers>`
    - src/app/layout.tsx contains `<Toaster`
    - `npm run build` exits 0
    - out/ directory exists with static export files
  </acceptance_criteria>
  <done>Frontend builds to static export. Warm dark palette with Iosevka monospace font applied. All shadcn CSS variables defined. Layout has Providers and Toaster wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static site build | Next.js produces static HTML/JS/CSS; no server-side code runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02a | Information Disclosure | next.config.mjs | accept | Static export mode has no server-side secrets. No API routes, no SSR. |
| T-01-02b | Tampering | package.json dependencies | accept | Dependencies are standard, well-maintained libraries. Lock file pins versions. |
</threat_model>

<verification>
- `npm run build` succeeds and produces `out/` directory
- globals.css has warm dark HSL values (not cool/blue grays)
- Iosevka font files present in node_modules/@fontsource/iosevka
- shadcn components exist in src/components/ui/ (button, dialog, sidebar, sonner, separator, skeleton, badge)
</verification>

<success_criteria>
Next.js static export builds successfully. The warm dark palette CSS variables match UI-SPEC.md values. Iosevka is the primary font. shadcn component library is initialized and ready for use. React Query provider wraps the app.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
